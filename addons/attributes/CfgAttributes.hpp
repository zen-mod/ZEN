class ADDON {
    class Object {
        class Attributes {

        };
        class Buttons {

        };
    };
    class Group {};
    class Waypoint {};
    class Marker {};
};

/*
class Attribute {
    displayName = ""; // Text displayed by the attribute's label
    tooltip = ""; // Tooltip text of the attribute's label
    control = ""; // Classname of the control used by this attribute
    valueInfo[] = {}; // Extra parameters used by the controls, varies based on control type
    condition = ""; // Code that must return true for the attribute to be shown (optional)
    statement = ""; // Code called on confirmation and the attribute was modified (generally)
    defaultValue = ""; // Code that returns the default value of the attribute
};
*/
